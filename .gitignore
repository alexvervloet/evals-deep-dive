# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets — never commit your real keys
.env

# Editor / local history
.history/
.vscode/

# Saved eval runs (regenerate them; they're outputs, not source)
runs/
*.run.json
